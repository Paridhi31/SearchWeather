# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GS-Weather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift'
  # Pods for GS-Weather

  target 'GS-WeatherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GS-WeatherUITests' do
    # Pods for testing
  end

end
